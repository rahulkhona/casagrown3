#!/bin/sh

# Pre-push hook: Run full test suite before pushing
# Ensures nothing is broken before code reaches the remote.

echo "🔍 Running pre-push checks..."

# 1. Run all app unit tests
echo "📋 Running app unit tests..."
yarn workspace @casagrown/app jest --passWithNoTests --bail || {
    echo "❌ App tests failed — push aborted."
    exit 1
}

echo "✅ All pre-push checks passed!"
