#!/bin/sh

# Pre-push hook: Run linting and type checks
# Note: Maestro E2E tests are run manually, not on pre-push

echo "🔍 Running pre-push checks..."

# Run lint-staged (already configured in package.json)
echo "✅ Pre-push checks complete (lint-staged runs automatically)"
