
# Ensure Maestro is available
export PATH="$HOME/.maestro/bin:$PATH"

if ! command -v maestro &> /dev/null
then
    echo "❌ Maestro not found! E2E tests are required."
    echo "👉 Install it: curl -Ls \"https://get.maestro.mobile.dev\" | bash"
    exit 1
fi

echo "🔄 Preparing Test Environment..."
./scripts/reset-and-seed.sh || exit 1

echo "🏃 Running Maestro E2E Tests..."
maestro test .maestro/home_screen_flow.yaml
