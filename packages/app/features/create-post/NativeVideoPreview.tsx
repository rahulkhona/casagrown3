/**
 * NativeVideoPreview — Web stub
 *
 * On web, video previews are handled with the HTML <video> element directly.
 * This stub is never rendered but exists to satisfy the import.
 */

import React from 'react'

export function NativeVideoPreview(_props: { uri: string }) {
  return null
}
