/**
 * Standalone Layout — pages without the main Navbar & BottomNav
 * Used for: /testers, landing pages, public forms
 */
export default function StandaloneLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
